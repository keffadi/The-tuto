### usefull command for asterisk

```
CLI> sip show peers
```

```
/usr/sbin/asterisk -r    (permanent when Asterisk already running)
/usr/sbin/asterisk -c
CLI> module reload chan_sip.so 
CLI> module reload chan_iax2.so
```
```
CLI> sip show peers 
CLI> sip show users 
CLI> iax2 show peers 
CLI> iax2 show users

dialplan reload

sip show peer serverB

module show like app_amd

```
